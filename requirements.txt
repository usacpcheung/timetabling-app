# Core web framework used by the app (templates, routes, dev server)
Flask>=2.2,<3.1

# PuLP modeling interface used for formulating MILP scheduling problems
pulp
# Default MILP solver backend (HiGHS); restricted to interpreters with binary wheels
highspy; python_version < "3.14"
# Alternate MILP solver backend (CBC) for environments where HiGHS wheels are unavailable
coin-or-cbc

# Optional Google OR-Tools CP-SAT backend (install if you need CP-SAT features and run on supported Python versions)
ortools; python_version < "3.14"

# Test runner used by the test suite in `tests/`
pytest>=7.0,<9.0
